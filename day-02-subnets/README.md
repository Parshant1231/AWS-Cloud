# How create Subnets in vpc

## Steps
1. first we have to pick a name for given subnet
2. Then pick a cidrs block "10.0.1.0/24" under given vpc "/16"
3. If you want subnet to be public then pick auto assign public IP's as for private don't tick.


# Why public Ip automatically gets a public IP automatically. Private sb not?
Reason = due to enabling public ip launch.